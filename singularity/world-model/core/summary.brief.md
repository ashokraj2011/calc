> **Grounding** · calc @ `0d8703c49dc3ca79c684d93cc42220c922d7cd15` · view: `core` · tier: `brief`
> **Generated** 09 August 2026 (2026-08-09T22:30:28Z) · depth: `quick` · builder `2.0`
> **Authoritative for:** file locations, entry points, commands, structural relationships as of the commit above.
> **Not authoritative for:** current file contents. If this document conflicts with code you have read, trust the code and say so explicitly in your output.
> **Unknowns are marked.** Do not resolve them by inference. If the repository has changed since the date above, treat locations as hints, not facts.

This repository is a browser-based React + Vite calculator app named ApexCalc. The main shell lives in `src/App.jsx`, where the app routes five modes—standard, scientific, converter, financial, and grapher—and persists theme, sound, and history in browser `localStorage`. Shared math, formatting, unit conversion, and financial helpers live in `src/utils/evaluator.js`, while feature-specific UI panels live in `src/components/`. The primary entry point is `index.html` → `src/main.jsx` → `src/App.jsx`. Standard validation commands are `npm run build` and `npm run lint`; build is the only one we observed to complete successfully in this checkout, while lint currently reports issues in several components. The single largest risk is that the app has no automated test harness and depends heavily on browser-only state and local persistence.
